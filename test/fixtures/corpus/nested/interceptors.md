### Interceptors

An interceptor is a class annotated with the `@Injectable()` decorator and implementing the `NestInterceptor` interface.

#### Aspect interception

Interceptors have a set of useful capabilities inspired by aspect-oriented programming.
